问题1：

  使用 v-if 切换逻辑复杂的控件的时候，切换的时候不重新渲染会出现问题，比如：两个表格长度不一，代码会自动补充，导致错误。
  
  解决方案：给需要切换的控件加 key 属性，来唯一标示，被key标示后会重新渲染。
  
问题2：
  
  编辑表格使用 Obj.assion(obj,{}); 将整行的值传入表格进行修改，导致所编辑内容与表格同步变化。
  
  解决方案:需要 “深度克隆” var newObj = Json.parse(JSON.stringify(row)); 
    
    
